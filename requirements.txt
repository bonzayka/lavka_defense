# Детектор 18+ — ViT-классификатор на onnxruntime (torch/transformers НЕ нужны)
aiogram>=3.15
aiohttp_socks>=0.9
Pillow>=11
numpy>=2
onnxruntime>=1.20
# Юзербот для полного скана участников (/scanall, чистка удалёнок). Опционально,
# но без него бот работает — просто /scanall недоступен.
telethon>=1.36
# Анти-деанон: OCR картинок на чужие персональные данные (модуль deanon.py).
# Опционально — без него проверка тихо отключается, бот работает как раньше.
# rapidocr на onnxruntime (уже в стеке), НЕ требует системного tesseract, rus+eng.
rapidocr-onnxruntime>=1.2
# Telegram Mini App (веб-покер, папка webapp/): FastAPI + uvicorn + WebSocket.
# uvicorn[standard] тянет websockets/httptools. Нужны, только если WEBAPP_ENABLED=1.
fastapi>=0.110
uvicorn[standard]>=0.29
